{
  "version": 2,
  "builds": [
    {
      "src": "api/generate.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "index.html"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/generate" }
  ]
}
